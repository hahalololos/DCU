# Qwen3.5-27B 12 小时快速收益冲刺计划

时间：2026-07-14 09:34 CST
源码基线：`vllm_cscc@0e3450b`

## 目标与策略

- 当前榜单 `84.9218` 分、第 52 名，距当前前 20 门槛 `2.9198` 分。
- 12 小时内形成 1--2 个可提交候选；单次预测增分至少 `+0.30`，组合争取
  `+1.0` 以上，SLA 扣分保持 0。
- bitwise 候选完成三档 A/B 后可先提交榜单，再补完整精度；非 bitwise 候选必须先完成
  四类精度门禁。
- 不重做 TILE64、GDN、小算子融合、UA2D 编译常量扫描和 MLP-down 专用 GEMV。

## 路线一：UA2D 实验 8

- 保持 `TILE32/BLOCK_M32/warps2`、KV tile 顺序和 online-softmax 更新顺序。
- 将完全可见的物理 KV block 与 causal 边界 block 拆环，完整 block 删除逐 tile causal
  判断和 mask。
- 在 QK、softmax 概率计算后再加载 V，缩短 V/S/P 的重叠 live range。
- 实验 5 保持默认与回滚；实验 8 通过
  `VLLM_ROCM_QWEN_UA2D_EXPERIMENT=8` 选择。
- 8K/16K/32K micro 至少达到 `1.05x/1.08x/1.10x`，加权至少 `1.08x`，且相对
  实验 5 bitwise 一致，才进入服务 A/B。

## 路线二：gfx936 GEMV V5 与 LM Head 探针

- V5 使用 512 threads、8 waves、两 wave/行、四行/workgroup；一次 LDS activation
  staging 供四行复用。
- 模式 3按三个生产形状逐项选择 V4/V5；V5 必须在三个种子均快至少 `2%`。
- 三形状按真实调用次数累计节省不足 `0.50 ms/token` 时停止。
- 同一轮构建只做 LM Head `(248320,1,5120)` micro；只有相对 `F.linear` 三种子均
  `>=1.08x` 且预测增分 `>=0.30` 才接入生产。

## 服务、精度与提交门禁

- 三档各 `10/10` 完成，输入/输出 token 与文本和对照一致。
- P99 TTFT 回退不超过 `2%`，P99 TPOT 回退不超过 `1%`，单档 output throughput
  回退不超过 `0.3%`。
- 中档提升 `>=0.3%` 或长档提升 `>=0.8%` 才补齐三档；预测增分 `>=0.30` 才提交。
- 收益小于 `0.5%` 的档位补跑反向顺序，以两轮中位数决策。
- 提交后补跑四类 OpenCompass；任一精度系数低于 `1.00` 或榜单回退，立即恢复最后一个
  无扣分版本。

## 执行顺序

1. 远端环境、源码和当前基线确认。
2. UA2D 实验 8实现、正确性、三档 micro、中长档服务门禁。
3. GEMV V5/LM Head micro、增量构建和短中档服务门禁。
4. 胜出候选组合三档验证、榜单提交、精度与回滚检查。
5. 每次源码优化和测试结果按时间戳追加至 `docs/progress.md`。
