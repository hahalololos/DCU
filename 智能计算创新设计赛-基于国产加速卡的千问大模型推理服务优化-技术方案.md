# 2026年全国大学生计算机系统能力大赛

## 智能计算创新设计赛(先导杯)

### 基于国产加速卡的Qwen大模型推理服务优化 赛题技术方案

## 一、赛题总体介绍

**第1条** 随着大语言模型进入工程部署落地阶段，推理服务性能已成为影响部署成本、服务稳定性和用户体验的关键因素。尤其在长上下文与混合长度场景下，随着输入序列长度的增加，单请求的资源需求急剧上升。KV Cache 占用随上下文长度线性膨胀，HBM 带宽压力与显存碎片问题突出，容易导致首 token 时延 (TTFT) 上升、输出吞吐下降。当前统一的 vLLM 推理框架具备基础的处理能力，但在长上下文单请求负载下，默认机制仍难以充分挖掘国产加速卡（本赛题采用 DCU）的计算与带宽潜力，兼顾吞吐、时延与显存占用。



**第2条** 参赛者需在统一模型权重、统一容器镜像、统一 vLLM 0.18.1 推理框架和统一服务接口条件下开展评测。优化方案的核心目标是：**在 TTFT P99 和 TPOT P99 均满足规定 SLA 门限的约束前提下，最大化输出吞吐量，并改善显存使用效率与服务稳定性。** SLA 为硬性约束，违反则该档位吞吐量得分直接清零。



**第3条** 本赛题的性能优化聚焦于**不同输入长度的单请求在线推理服务**。参赛者需结合 Qwen3.5-27B 模型特点、长上下文输入特征和 DCU 硬件特性，采取针对性优化策略，包括但不限于：KV Cache 与显存管理优化、执行路径与算子优化，以及在允许边界内的精度无损压缩方案。



**第4条** 镜像预装内容固定为 Python 3.10.12、指定版本 PyTorch 2.10.0、vLLM 0.18.1、固定版本 transformers 5.5.0、Qwen3.5-27B 官方 tokenizer、官方 chat template 和官方 bf16 原始权重以及官方基准测试工具。选手可叠加自定义层，包括安装 Python 包、编译 custom kernel，但需提交改动说明。最终评测仅在统一容器环境内执行，宿主机不注入任何额外库，评测期间禁止从外部网络下载任何依赖。

## 二、初赛要求与评分标准

**第5条** 本赛题初赛聚焦真实场景下的在线推理服务效率优化，主要挑战维度如下：

1. **KV Cache 与显存管理优化**
   - **高效块管理与碎片化控制**：设计更高效的 PagedAttention 块分配与回收机制，针对长上下文请求优化块粒度与对齐策略，降低单请求内的显存碎片率与 HBM 访问开销。
2. **框架与算子优化**
   - **(1) Decode 阶段算子优化**：针对生成阶段的 Attention 算子、Linear 算子，结合 DCU 的显存带宽与计算特性，进行算子融合与内核优化，降低每 Token 生成耗时 (TPOT)。
   - **(2) 执行路径优化**：减少 Python 层调度开销、降低 kernel launch 延迟，优化调度逻辑与内存拷贝路径，缩短首 Token 时延 (TTFT)。
   - 

**第6条** 本赛题初赛建议的优化内容包括：

1. Decode 阶段调度进行深度定制。
2. 允许对 KV Cache 的分配机制、显存预算控制和块管理策略进行优化。
3. 允许结合 DCU 的显存容量、带宽、片上缓存层级、并行执行特征和数据搬运开销，对资源组织调度方式进行适配优化。



**第7条** 本赛题初赛**禁止**的行为包括：

1. 不得通过修改采样参数（如强行调低 Max Tokens）、截断输入、跳过长样本、过滤困难样本、不得跳过层等方式规避真实推理开销。
2. 不得通过预缓存测试集与答案、预生成中间结果等针对性优化手段响应请求。
3. 不得绕开统一服务接口、评测流程与资源统计路径，不得引入题目规定外的辅助模型（如外挂自行训练的小模型用于投机采样等）。
   - **(1) 严禁修改模型结构**，替换模型权重、改变模型推理语义或输出口径。不得通过后训练、蒸馏、微调等方式修改模型。
   - **(2) 量化操作边界**：禁止在权重加载前、权重加载后、服务初始化阶段或正式推理前，对模型权重进行持久化量化、结构化剪枝、权重重排压缩、模型图重构、模型格式转换或生成可复用量化权重缓存。（**允许**推理过程中的非持久化、算子级低精度计算优化，例如激活值动态量化、KV Cache 量化、kernel 内部临时类型转换、低精度矩阵乘法、Attention 内核优化等。）
   - **(3) 模型剪枝边界**：禁止任何形式的结构化剪枝、非结构化剪枝、动态通道跳过、动态层跳过、注意力头裁剪、token pruning、early-exit 等改变模型有效计算结构或输出语义的行为。
   - **(4) 推理过程中不得生成**可供后续样本复用的新模型文件、量化权重文件、剪枝权重文件或压缩权重缓存。



**第8条** 初赛主要扣分与失效点如下：

1. **SLA 熔断**：吞吐提升明显，但 TTFT、TPOT（每个输出 Token 的平均耗时）的 P99 时延严重超标，或服务完成率下降超过 1%，该档位吞吐得分将直接清零。
2. 方案与显存、带宽、访存等物理约束脱节，或纯粹针对特定评测数据集写死的 If-Else 过拟合规则。



**第9条** 初赛评价指标（总分 100 分）如下：

1. 初赛评估参赛方案在指定国产加速卡 (DCU) 单卡环境下的实际服务能力。评测数据集由 LongBench 和 RULER 子集混合重组，**初赛题并发数固定为 1**。
2. 按输入长度划分为三个区间：**4k-8k、8k-16k、16k-32k**。任务类别参考现有长文本评测集的任务形态：问答和摘要参考 LongBench 的文档 QA 与摘要任务；检索和聚合参考 RULER 的增强检索任务与信息聚合任务。请求分布特征参考如下：

| **任务类别** | **数据集选择** | **任务范围**               | **评分指标** |
| ------------ | -------------- | -------------------------- | ------------ |
| 问答         | LongBench      | 单文档 QA、多文档 QA       | Exact Match  |
| 摘要         | LongBench      | 单文档长文摘要、多文档摘要 | ROUGE-L      |
| 检索         | RULER          | 单点检索、多点检索         | Accuracy     |
| 聚合         | RULER          | 关键词聚合、数值统计       | Accuracy     |

1. **评测相关指标定义**

   为避免统计口径争议，评测时所有核心指标遵循以下严格定义：

   - **(1) 性能评测**：统一使用 `vllm bench serve` 完成，用于对参赛者部署的推理服务进行压测，并统计服务性能相关指标，包括输出吞吐量 (Throughput)、首 Token 时延 (TTFT)、TPOT。

   - **(2) 精度评测**：统一使用 `OpenCompass` 完成，用于对问答、摘要、检索、聚合等任务的输出结果进行精度评估。精度评测结果用于精度扣分计算，不参与吞吐量或时延指标统计。

   - **(3) 吞吐量 (Throughput)**：严格定义为输出吞吐量 (Output Tokens / Second)。计算逻辑为：

     $$\text{吞吐量} = \frac{\text{所有完成请求的生成 Token 总数}}{\text{最后一个请求完成的时间点} - \text{第一个请求发出的时间点}}$$

     输入的 Prompt Token 不计入吞吐量分子。

   - **(4) TTFT (Time To First Token, 首 Token 时延)**：采用客户端端到端计时。

     - **起算点**：评测客户端向推理服务 API 发出 HTTP 请求的绝对时间戳。
     - **终止点**：评测客户端收到流式返回第一个生成 Token 的绝对时间戳。
     - **包含内容**：TTFT 明确包含 Tokenizer 编码时间以及首 Token 的 Prefill 计算时间。初赛题并发数固定为 1，引擎内无排队等待时间。

   - **(5) TPOT (Time Per Output Token, 每 Token 生成耗时)**：对每条请求，定义为从收到第一个 Token 到收到最后一个 Token 的时间差，除以（输出 Token 数 - 1）。将全部请求的 TPOT 值汇入全局池后取 P99 分位值 (TPOT P99)。输出 Token 数为 1 的请求不参与 TPOT 统计。

   - **(6) P99 统计维度**：

     - **TTFT P99**：按请求维度统计，取全部请求首字时延的 P99 分位值，各档位分别计算，各档独立判断是否达标。
     - **TPOT P99**：所有请求的 TPOT 值汇总后取 P99 分位值，全局统一（不分档），适用于所有档位的 SLA 判断。

   - **(7) 官方 Baseline 核心参数示例（评测时严格锁定）**：

     `vllm serve Qwen/Qwen3.5-27B --max-model-len 32768`

     以下参数严格锁定：`max_tokens`、`temperature=0`、`max-num-seqs`。

   - **(8) 不允许调整的参数**（所有影响任务定义、上下文范围和输出行为的参数严格锁定）。以下按类别列出不可调整项；若与本说明存在差异，以评测脚本与实际评测命令的锁定为准。

     - **模型与分词**：模型权重 (model)、tokenizer、tokenizer-mode、chat template。
     - **上下文与批处理**：`--max-model-len`、`--max-num-seqs`、`--max-num-batched-tokens` 及其它 batch scheduler 相关参数（包括不允许 vLLM 内部 batch scheduler 相关代码修改）。
     - **禁止能力**：任何形式的投机解码，包括但不限于 draft model、MTP、多头预测、外挂小模型、自训练预测器、基于中间层的 early-exit draft、预生成 token 序列缓存等。参赛方案必须基于官方模型的标准自回归解码流程完成输出生成。
     - **Bench 与结果**：bench 的 percentile/percentiles 统计口径、结果保存选项、结果解析脚本。
     - **服务接口**：`--served-model-name`、OpenAI API 路径、请求/响应格式（host/port/路由由平台固定）。

2. **输出吞吐量（总计 100 分）**

   - 吞吐量评分基于统一的、提前评测验证的官方基准线 (Baseline) 计算相对提升，杜绝调参套利。
   - 在 SLA 门限通过的前提下，按三档输入长度分别计算吞吐提升得分，三档权重分别为 **20%、50%、30%**，各档满分分别为 20 分、50 分、30 分，合计 100 分。
   - 单档相对提升率 = $\frac{\text{参赛方案该档吞吐量} - \text{Baseline 该档吞吐量}}{\text{Baseline 该档吞吐量}}$
   - 各档得分 = $\text{该档满分} \times \left( 60\% + 40\% \times \left(1 - e^{-1.3 \times \text{单档相对提升率}}\right) \right)$

3. **时延 SLA 熔断机制（硬性约束，不另计分值）**

   - 服务可用性通过首 Token 时延 (TTFT) 和每 Token 生成耗时 (TPOT) 联合评估，仅作为硬性约束，不另计得分。
   - **SLA 门限**：
     - $\text{TTFT P99（各档分别）} \le \text{Baseline TTFT P99（对应档）} \times 1.5$
     - $\text{TPOT P99（全局）} \le \text{Baseline TPOT P99（全局）} \times 1.5$
   - **熔断机制**：各不同上下文长度档位的吞吐测试中，一旦上述任一 SLA 指标未能达标，**该档位对应的吞吐量得分直接清零**。

4. **约束条件：模型精度**

   - 初赛精度评估与吞吐评测使用同一套任务类型与评分口径，统一要求如下：
     - 评测时 $\text{Temperature} = 0.0$；
     - 所有任务均与官方 Baseline 输出结果或标准答案进行对比；
   - 对四类任务分别计算精度结果，并统计相对 Baseline 的性能下降幅度，记为 $\Delta$，根据下表得到各任务对应精度系数 $k_i$，取四类任务系数均值作为最终精度系数。

| **精度下降幅度 Δ**          | **精度系数 k**      |
| --------------------------- | ------------------- |
| $\Delta \le 1.0\%$          | 1.00                |
| $1.0\% < \Delta \le 2.0\%$  | 0.97                |
| $2.0\% < \Delta \le 3.0\%$  | 0.94                |
| $3.0\% < \Delta \le 5.0\%$  | 0.90                |
| $5.0\% < \Delta \le 10.0\%$ | 0.85                |
| $\Delta > 10\%$             | 0（该类任务不计分） |

$$\Delta = \frac{\text{Baseline 精度} - \text{参赛方案精度}}{\text{Baseline 精度}} \times 100\%$$

$$\text{最终得分} = \text{吞吐量得分} \times \frac{1}{4} \times \sum_{i=1}^{4} k_i$$

## 三、决赛要求与评分标准

**第10条** 决赛形式为线下比赛和答辩，决赛各环节的要求、安排和评分标准在决赛阶段另行公布。

## 四、软硬件系统规范

**第11条** 参赛者在进行模型优化和部署时，需使用以下工具和环境：

1. **硬件环境**：初赛使用指定国产 x86 处理器及 DCU 加速卡（单卡评测环境）；决赛采用同型号多卡分布式评测环境，具体卡数由组委会赛前统一公示。
2. **软件环境**：Python 3.10.12、固定版本 PyTorch 2.10.0、vLLM 0.18.1、transformers 5.5.0、Qwen3.5-27B (bf16) 官方原始权重。
3. **盲测公平性**：统一使用测试集和请求流。评测为黑盒盲测，参赛者无法提前获知完整的样本内容和请求先后顺序，杜绝针对特定 Prompt 的缓存作弊。发现任何伪造结果、隐藏异常等违背技术道德的行为，立刻取消成绩。

## 五、参赛作品提交要求

**第12条** 源码提交与编译要求：

1. **完整源代码**：包含所有源代码和编译脚本，代码规范，确保在指定环境中能够顺利运行。代码需具有良好的注释和清晰的结构，便于评审人员理解和复现。
2. 参赛队伍应在测评机器上完成源码编译，并保证编译成功。评测平台将在统一容器环境下执行编译，若编译失败，平台将输出编译错误日志 (log) 并返回给参赛队伍，队伍需在规定时间内修复后重新提交。



**第13条** 环境变量说明文档：

若参赛方案依赖自定义环境变量，需随代码一并提交环境变量说明文档，文档须包含以下内容：

1. 配置了哪些环境变量（逐项列出变量名与取值）。
2. 每个环境变量的作用及配置原因。
3. 未提交说明文档但实际使用了自定义环境变量的方案，视为提交不完整，评测结果可能无效。



**第14条** 优化方案说明文档：

参赛队伍须提交优化方案说明文档，需清晰描述：

1. 所采用的优化方法与技术路线。
2. 各项优化措施对性能提升的贡献分析。
3. 优化点汇总表。
   - 文档格式不限，建议含关键代码路径说明及对应的性能对比数据。



**第15条** 所有优化需支持在组委会平台正确运行。参赛队伍必须严守学术诚信。若使用第三方知识产权、开源代码、公开算法或第三方库，以及引用他人源码等，必须在提交的说明文档及源码项目 `README.md` 文件头部进行明确标注与说明。

## 六、比赛网站与参考资源

**第16条** 比赛网站：https://pra.xtnl.org.cn/。



**第17条** 比赛网站提供多种软件开发工具及设计资料，包括但不限于下列内容：HIP 程序设计语言规范及说明、DTK 软件使用说明、竞赛平台（代码提交方式、代码提交示例、竞赛平台使用）说明文档、硬件资源调试和使用指南等。

## 七、其他

**第18条** 本技术方案的最终解释权归 2026 年全国大学生计算机系统能力大赛智能计算设计赛（先导杯）技术委员会。
